# قرارداد commit

از الگوی `type: summary` استفاده کنید: `feat` برای قابلیت، `fix` برای رفع اشکال، `style` برای ظاهر، `docs` برای مستندات، `ci` برای pipeline و `chore` برای امور نگهداری. هر پیام باید کوتاه و بیانگر یک تغییر باشد.
