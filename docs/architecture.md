# معماری

این پروژه build step ندارد. `index.html` ساختار، `assets/css/style.css` ظاهر و `assets/js/app.js` تعامل‌ها را نگه می‌دارند. دادهٔ taskها و ترجیح پوسته فقط در `localStorage` مرورگر ذخیره می‌شود.
